/** No illustrative records here -- the app fetches real data from Firestore
 * (`opportunities`, `activities`, `collaborators`). These stay empty until
 * that data loads, rather than showing placeholder organisations. */
export const donorOpportunities = []

export const collaboratorDirectory = []

export const mhubActivities = []
