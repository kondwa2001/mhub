import { Icon } from './Icon'
export function DonorCard({ donor, saved, onSave, onOpen }) { return <article className="donor-card"><div className="card-top"><span className={`donor-logo ${donor.tone}`}>{donor.initials}</span><button onClick={() => onSave(donor.id)} className={saved ? 'save-button saved' : 'save-button'} aria-label={`Save ${donor.name}`}><Icon name="bookmark" /></button></div><button className="donor-card-main" onClick={() => onOpen(donor)} aria-label={`View ${donor.name} details`}><span className="match"><i></i>{donor.match == null ? 'Directory listing' : `${donor.match}% match`}</span><h3>{donor.name}</h3><p>{donor.focus}</p></button><div className="card-footer"><span>{donor.region}</span><button className="details-button" onClick={() => onOpen(donor)}>View details <Icon name="arrow" /></button></div></article> }
