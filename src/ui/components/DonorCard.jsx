import { Icon } from './Icon'
export function DonorCard({ donor, saved, onSave }) { return <article className="donor-card"><div className="card-top"><span className={`donor-logo ${donor.tone}`}>{donor.initials}</span><button onClick={() => onSave(donor.id)} className={saved ? 'save-button saved' : 'save-button'} aria-label="Save opportunity"><Icon name="bookmark" /></button></div><div><span className="match"><i></i>{donor.match}% match</span><h3>{donor.name}</h3><p>{donor.focus}</p></div><div className="card-footer"><span>{donor.region}</span><strong>{donor.deadline === 'Rolling' ? 'Rolling' : `Due ${donor.deadline}`}</strong></div></article> }
