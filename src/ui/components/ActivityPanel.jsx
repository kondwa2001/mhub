import { Icon } from './Icon'
export function ActivityPanel({ activities }) { return <section className="activities"><div className="section-heading"><div><p className="eyebrow">FROM THE HUB</p><h2>What MHub is doing</h2></div><button className="text-button">All activities <Icon name="arrow" /></button></div><div className="activity-list">{activities.map((activity) => <article className="activity" key={activity.title}><div className="date-block"><strong>{activity.date}</strong><span>{activity.month}</span></div><div className="activity-main"><span className="activity-type">{activity.type}</span><h3>{activity.title}</h3><p>{activity.copy}</p></div><span className={activity.status === 'This week' ? 'status now' : 'status'}>{activity.status}</span><button className="round-arrow" aria-label={`View ${activity.title}`}><Icon name="arrow" /></button></article>)}</div></section> }
